package com.chainnet.navigation

enum class Screen(val route: String, val label: String) {
    Home("home", "Home"),
    Topology("topology", "Topology"),
    Pairing("pairing", "Pairing"),
    Settings("settings", "Settings")
}
